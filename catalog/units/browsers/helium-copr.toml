id = "browsers-helium-copr"
description = "Helium browser bundle from COPR for Fedora"
config = ["browser-theme"]

[choice]
category = "browsers"
id = "helium"
label = "Helium Browser"
order = 60
description = "Privacy-first browser with built-in ad and tracker blocking"

[[install]]
backend = "dnf"
sources = ["copr:imput/helium"]
packages = ["helium-bin"]

[[install]]
backend = "action"
actions = ["browser-theme:helium"]
