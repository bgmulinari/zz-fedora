id = "base-ghostty"
description = "Ghostty terminal bundle for Fedora"
config = ["ghostty"]

[base]
order = 90

[[install]]
backend = "dnf"
sources = ["terra"]
packages = ["ghostty", "ghostty-shell-integration"]
