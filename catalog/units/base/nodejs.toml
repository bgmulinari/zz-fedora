id = "base-nodejs"
description = "Node.js and npm runtime for JavaScript-based CLI tools, with global packages in ~/.local"
config = ["npm"]

[base]
order = 210

[[install]]
backend = "dnf"
packages = ["nodejs24-npm"]
