# Managed by zz-linux-setup.
#
# Noctalia v5 loads hand-written config from ~/.config/noctalia/*.toml.
# Noctalia writes GUI/runtime overrides to ~/.local/state/noctalia/settings.toml,
# so do not stow generated lockscreen or widget placement state here.

[shell]
polkit_agent = true
telemetry_enabled = false

[location]
auto_locate = true

[wallpaper]
directory = "~/.local/share/backgrounds"

[wallpaper.default]
path = "~/.local/share/backgrounds/BlueTide.jpg"

[bar.default]
start = [ "launcher", "settings", "wallpaper", "workspaces", "taskbar" ]
center = [ "clock", "weather" ]
margin_edge = 0
margin_ends = 0
radius = 0
radius_bottom_left = -10
radius_bottom_right = -10
background_opacity = 0.9

[dock]
background_opacity = 0.9

[notification]
background_opacity = 0.9

[osd]
background_opacity = 0.9

[widget.clock]
format = "{:%b %d %H:%M}"

[widget.media]
hide_when_no_media = true

[widget.network]
show_label = false

[widget.taskbar]
only_active_workspace = true
show_window_title = true

[widget.weather]
show_condition = false

[theme]
mode = "dark"
source = "builtin"
builtin = "Nord"

[theme.templates]
enable_builtin_templates = true
builtin_ids = [
  "niri",
  "ghostty",
  "starship",
  "btop",
  "gtk3",
  "gtk4",
  "qt",
  "kcolorscheme",
]
enable_community_templates = true
community_ids = [
  "pywalfox",
  "zen-browser",
  "neovim",
  "vscode",
  "zed",
  "yazi",
]

[theme.templates.user.icon_theme]
input_path = "$XDG_CONFIG_HOME/noctalia/templates/icon-theme-accent"
output_path = "$XDG_CACHE_HOME/noctalia/icon-theme-accent"
post_hook = "$HOME/.local/bin/noctalia-sync-icon-theme"
