input {
    // Mouse, touchpad, and keyboard device settings are generated by DMS in
    // ~/.config/niri/dms/input.kdl so its Settings UI can change them.
    focus-follows-mouse
    workspace-auto-back-and-forth
}
